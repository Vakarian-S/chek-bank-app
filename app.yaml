runtime: nodejs16

service: chek-bank-app

handlers:
  - url: /(.*\.(js|css|svg|png)(|\.map))$
  static_files: dist/\1
  upload: dist/(.*)(|\.map)
    - url: /.*
  static_files: dist/index.html
  upload: dist/.*
