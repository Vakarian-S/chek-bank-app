runtime: nodejs
env: flex
runtime_config:
  nodejs_version: 16
service: chek-bank-app
env_variables:
  BUCKET_NAME: "your-ng-app-bucket"
handlers:
  - url: /
    static_files: dist/chek-bank-app/index.html
    upload: dist/chek-bank-app/index.html

  - url: /
    static_dir: dist/chek-bank-app
